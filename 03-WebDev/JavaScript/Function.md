#函数
## 箭头函数：
### 注意事项：
- 箭头函数是不会创建自己的`this`指针的
	- 它的`this`来自于它**被定义时继承的外层作用域内的`this`**
	- 没有`this`，所以不能使用`call(),bind(),apply()`方法来改变`this`的指向
- 箭头函数是没有原型的
	- 不能作为构造函数使用
	- 不能使用`super()`
- 箭头函数是没有`arguments`对象的
