这篇笔记主要记录如何封装一个功能详尽的网络请求模块。
## 设计思路：
一个完整的网络请求模块应该具有以下基本功能：
- 核心层：封装`axios`实例、`baseURL`、`header`、`timeout`
- 拦截器层：施加请求/响应拦截，附加`token`、时间戳。统一处理错误响应。
- 业务封装层：集中导出通用方法：`get`,`post`...等常用请求方式。
- 功能增强层：请求的缓存、超时自动重试、请求取消、loading状态管理。
- 进阶工具层：记录错误日志、上报异常、mock数据支持。